document.addEventListener('DOMContentLoaded', () => {
    // Note: Global UI (sidebar, header, icons, profile dropdown) 
    // is now handled completely by scripts/layout.js.
    
    // Future profile-specific logic goes here!

    console.log("Profile page initialized.");
});